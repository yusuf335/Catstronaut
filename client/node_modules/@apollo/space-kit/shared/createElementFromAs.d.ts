import * as React from "react";
export declare type As = React.ReactElement | keyof JSX.IntrinsicElements;
/**
 * Take an `as` value and make it into a `React.ReactElement`
 */
export declare function createElementFromAs(as: As): React.ReactElement;
