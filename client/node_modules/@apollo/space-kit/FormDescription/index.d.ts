/** @jsx jsx */
/** @jsxFrag React.Fragment */
import * as React from "react";
interface Props extends Pick<React.DetailedHTMLProps<React.HTMLAttributes<HTMLDivElement>, HTMLDivElement>, "className" | "style" | "id"> {
    children: React.ReactNode;
}
/**
 * Component to render a form description.
 *
 * If this component is rendered in the children of `FormControl`, then
 * `FormControl` will render this element in it's layout. Otherwise, it's
 * rendered as-is.
 */
export declare const FormDescription: React.FC<Props>;
export {};
