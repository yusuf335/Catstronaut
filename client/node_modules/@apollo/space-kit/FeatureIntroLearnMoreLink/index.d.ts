/** @jsx jsx */
import * as React from "react";
import { As } from "../shared/createElementFromAs";
interface Props extends Pick<React.DetailedHTMLProps<React.HTMLProps<HTMLAnchorElement>, HTMLAnchorElement>, "className" | "style" | "id"> {
    /**
     * Override how the learn more link is rendered.
     *
     * @default "a"
     */
    as?: As;
}
/**
 * Component to render Feature Intro learn more button.
 *
 * If this component is rendered in the children of `FeatureIntroControl`, then
 * `FeatureIntroControl` will render this element in it's layout. Otherwise, it's
 * rendered as-is.
 */
export declare const FeatureIntroLearnMoreLink: React.FC<Props>;
export {};
