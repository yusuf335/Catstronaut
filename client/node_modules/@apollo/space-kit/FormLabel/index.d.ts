/** @jsx jsx */
/** @jsxFrag React.Fragment */
import * as React from "react";
interface Props extends Pick<React.DetailedHTMLProps<React.LabelHTMLAttributes<HTMLLabelElement>, HTMLLabelElement>, "aria-invalid" | "className" | "style" | "id" | "htmlFor"> {
    children: React.ReactNode;
    required?: boolean;
}
/**
 * Component to render a form label.
 *
 * If this component is rendered in the children of `FormControl`, then
 * `FormControl` will render this element in it's layout. Otherwise, it's
 * rendered as-is.
 */
export declare const FormLabel: React.FC<Props>;
export {};
